import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: {
    title: "Home",
    type: "page",
  },
  posts: {
    title: "Posts",
    type: "page",
  },
};

export default meta;
